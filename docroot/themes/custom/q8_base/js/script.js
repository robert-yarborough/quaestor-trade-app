/**
 * @file
 * Custom scripts.
 */
(function ($, Drupal) {
  Drupal.behaviors.d8base = {
    attach: function (context, settings) {
      // Write your custom scripts here.
    }
  };

})(jQuery, Drupal);